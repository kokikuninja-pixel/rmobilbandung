# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Meningkatkan maxInstances untuk menangani traffic dan redundansi saat rollout
  maxInstances: 10
  # Menambahkan cpu dan memory jika diperlukan (default biasanya cukup untuk NextJS dasar)
  # cpu: 1
  # memoryMiB: 512
